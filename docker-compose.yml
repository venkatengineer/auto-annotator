version: '3.8'

services:
  annotator:
    build: .
    image: auto-annotator:latest
    container_name: auto-annotator
    ports:
      - "5001:5000"
    volumes:
      - "${HOST_DATA_DIR:-./data}:/data"
    environment:
      - HOST_DATA_DIR=${HOST_DATA_DIR:-./data}
